#!/usr/bin/env zsh
#
# co
# Fuzzy git checkout
#-------------------------------------------------------------------------------

set -e

BRANCH=$(getgitbranch --header="Select a branch to checkout") &&
HUSKY=0 git checkout ${BRANCH}
