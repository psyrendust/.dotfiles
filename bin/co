#!/usr/bin/env zsh
#
# co
# Fuzzy git checkout
#-------------------------------------------------------------------------------

set -e

BRANCH=$(getgitbranch --header="Select a branch to checkout") &&
git checkout ${BRANCH}
