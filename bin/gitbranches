#!/usr/bin/env bash
#
# gitbranches
# List the last 20 branches sorted by last commit date in decending order (most recent on top)
#-------------------------------------------------------------------------------

set -e

git branch --sort=-committerdate | head -20
