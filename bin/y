#!/usr/bin/env zsh
#
# y
# Use the correct package manager.
#-------------------------------------------------------------------------------

set -e

pm="$(ywhich)"

if [[ -n "$pm" ]]; then
  $pm $@
fi
