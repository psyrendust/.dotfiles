#!/usr/bin/env zsh
#
# realpath2
# Get the real path (python os.path.realpath — resolves even missing paths)
#-------------------------------------------------------------------------------

set -e

function realpath() { python3 -c "import os,sys; print(os.path.realpath(sys.argv[1]))" "$1"; }
realpath "$@"
